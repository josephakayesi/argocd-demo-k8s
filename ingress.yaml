# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: web-app-ingress
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
# spec:
#   rules:
#     - host: web.local # or change to your domain
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: web-app
#                 port:
#                   number: 80
